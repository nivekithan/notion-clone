import { Utility } from "./";

const utility : Utility = {
    wrapper: [" "]
}

export const DeleteConformation = () => {
    return (
        <section className={utility.wrapper.join(" ")}>
            I am delete
        </section>
    )
}